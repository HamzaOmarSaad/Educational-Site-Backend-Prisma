export enum emailEnum {
  confirmEmail = "confirmEmail",
  forgetPass = "forgetPass",
  twoStepVerification = "twoStepVerification",
}
export enum redisPurposeEnum {
  OTP = "OTP",
  revokeToken = "revokeToken",
}
